const arr = [1, 2, 3, 4, 5, 6, 7, 8, 9];

function rBsearch(array, targetVal) {
  if (arr.length === 0) {
    return false;
  }
}
const midIdx = Math.floor(array.length / 2);
if (target === midIdx) return midIdx;
